# Streamlit configuration (applies locally and on Streamlit Community Cloud).

[theme]
# Warm "renovation / interior" palette — muted-earth (cream + clay).
base = "light"
primaryColor = "#b0764f"
backgroundColor = "#f8f3ea"
secondaryBackgroundColor = "#f0e8db"
textColor = "#34302a"
