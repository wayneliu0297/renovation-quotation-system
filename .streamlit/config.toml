# Streamlit configuration (applies locally and on Streamlit Community Cloud).

[theme]
# Pinned light theme + a professional blue palette (matches the portfolio site).
base = "light"
primaryColor = "#2563eb"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f5f7fb"
textColor = "#0f1729"
